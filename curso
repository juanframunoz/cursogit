GIT BASH 

$ git init - > iniciar repositorio
$ git status -> estado 
$ git add -A (añadir cambios de todos los archivos)
$ git add index.html (añadir cambios de un archivo concreto)
$ git commit -m "mensaje " -> Crea un commit o punto con el nombre del mensaje
$ git log -> nos da los listadocleras de comits que hemor realizado
$ git checkout [numero de commit que te aparece cuando haces git log]
$ git checkout master [se vuelve al último commit que hicimos]
$ git resset --soft [numero de coomit] -> elimina el commit, pero no el trabajo realizado
$ git reset --hard [numero de coomit] -> elimina el commit y los cambios en código
$ git branch -> nos dirá las ramas que hay
$ git branch [nombre] -> creará una rama del proyecto con el nombre
$ git checkout [nombre de la rama] -> trabajaremos en esa rama
$ git merge [nombre de la rama] -> fusionará las dos ramas
$ git branch [nombre de la rama] -d -> elimina la rama

---------------------

GITHUB

$ git clone [url github] clonará el proyecto en la carpeta local
$ git remote add origin [url github] hacemos git push origin master
$ git push origin master -> Fuerza la subida
